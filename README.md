# Team SR VIP â€” Step 1

Mobile-friendly local dashboard for statistical WinGo-style analysis.

## Run
Open `index.html` in a browser.

## GitHub Pages
1. Create a repository named `team-sr-vip`.
2. Upload `index.html`, `style.css`, `app.js`.
3. Repository Settings â†’ Pages.
4. Deploy from the `main` branch, root folder.
5. Open the generated Pages address.

## Important
This Step-1 build is a local demo. It does not connect to a betting platform, predict hidden server results, or guarantee wins. Step 2 can add historical-data import and source comparison; Step 3 can add backtesting.
